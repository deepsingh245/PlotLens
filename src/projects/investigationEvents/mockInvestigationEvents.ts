import type { InvestigationEvent } from "./types";

/** Track A placeholder — the timeline starts empty; events accrue as the user works. */
export const mockInvestigationEvents: InvestigationEvent[] = [];
