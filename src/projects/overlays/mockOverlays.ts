import type { ImageOverlay } from "./types";

/**
 * Track A placeholder data only — see docs/plans/plan-3.md Track B.
 * Empty: there's no sensible checked-in mock image to seed with (unlike
 * mockProjects.ts/mockAnnotations.ts, which have plain geometry/text to fake).
 * Replaced by real Firestore + Storage once a Firebase project exists.
 */
export const mockOverlays: ImageOverlay[] = [];
